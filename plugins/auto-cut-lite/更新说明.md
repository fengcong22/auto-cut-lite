# Auto-Cut Lite 更新说明

## 1.6.5+codex.20260903020653 — 2026-09-03

### 对应源码

- 分支：`feature/auto-cut-lite`
- 前置源码 Commit：`bc96b3d`
- 最终构建 Commit 记录在 ZIP 构建回执的 `source_git_commit` 字段中。

### 本次修复

- 反向 ASR 重复词裁决升级为逐命中回执。每个保留复现都绑定自己的短语、文本、起止时间、匹配方法、前/后/中间位置和独立上下文；严格校验一对一核对，拒绝缺失、重复、篡改或落入目标切口的回执。
- 彩色分段删词按项目契约中有序的 `delete_phrases` 验证，不再拿包含未标色文字的整句展示文本比对单个命中。两个彩色片段之间的未标色 ASR 文本自动加入 `must_keep` 及精确时间证据。
- 用真实争议点回归：`08：33` 保持 `513.44–513.64` 与 `513.76–514.00` 两个独立窗口并保护中间“就”；`09：28` 的 3 个保留“是”均通过各自可归因回执。
- 音频规划器、反向报告和可恢复任务版本已升级。源 ASR 与候选音频的原始 ASR 缓存可继续按媒体哈希复用，旧 V4 派生反向报告会按 V5 证据契约重建。

### Lite 交付边界

- 口播删除继续采用非破坏性的可编辑切口记录，源媒体保持完整，工程总时长不变。
- 本次只修复运行时、严格校验和部署包；不重新执行、不改写暂停任务的旧草稿或最终 ZIP。

## 1.6.4+codex.20260902192305 — 2026-09-02

### 对应源码

- 分支：`feature/auto-cut-lite`
- 前置源码 Commit：`0d6a5a6`
- 最终构建 Commit 记录在 ZIP 构建回执的 `source_git_commit` 字段中。

### 本次修复

- 修复同一条彩色删词意见包含多个独立 ASR 窗口时的严格校验冲突。每个剪辑编辑现在携带自己窗口的 ASR 匹配和边界证据，同时整条意见继续保留完整多窗口清单。
- 兼容 1.6.3 已生成的多窗口缓存：仅当彩色删词的窗口清单、ASR 清单、窗口序号和实际编辑边界全部一致时，允许把旧版合并边界解释为该条意见的总范围。
- 已用暂停任务中的 `08：33，删除蓝色字“我们就看”` 数据回归验证；两个删除窗口保持独立，中间未着色的“就”不进入删除范围。

### Lite 交付边界

- 口播删除继续采用非破坏性的可编辑切口记录，源媒体保持完整，工程总时长不变。
- 升级后可复用暂停任务已有的素材下载、源 ASR 和反向 ASR 缓存；本次封包与部署不重新执行真实剪辑项目。

## 1.6.3+codex.20260902174252 — 2026-09-02

### 对应源码

- 分支：`feature/auto-cut-lite`
- 前置源码 Commit：`84d4007`
- 最终构建 Commit 记录在 ZIP 构建回执的 `source_git_commit` 字段中。

### 本次修复

- 新增画面内容修改分类；画面删除和画面文字/数据修改进入 Visual 标记栏，动画方向或时序修改进入 Animation 标记栏，不再混入删词栏。
- 收紧审阅图片绑定条件；只有明确“用图片替换/替换为右图”等替换关系才导入素材，图片动画时序和参考图不再误贴入项目。
- 删词 ASR 在时间锚约束下支持固定口语插入“这个”，并让近处末字省略候选与远处完整候选共同参与时间选择。
- 区分用户明确填写和系统自动生成的必留词：明确必留词继续严格校验，自动邻接词未被反向 ASR 识别时记录告警；保护词落入删除窗口仍严格失败。
- 升级审阅编译、音频规划、反向 ASR 和飞书文档适配器版本，旧分类与识别缓存不会被继续复用。

### Lite 交付边界

- 画面内容与动画修改仍按 Lite 规则保留原文标签，不自动改画面。
- 口播删除仍采用非破坏性的可编辑切口记录，源媒体保持完整，工程总时长不变。
- 本次仅修复运行时和部署包，不重新执行历史真实剪辑项目。

## 1.6.2+codex.20260902103718 — 2026-09-02

### 对应源码

- 分支：`feature/auto-cut-lite`
- 当前源码 Commit：`a91a2b1e6b2a31c06321993a7a3de376d4d19a00`
- 包含的前置提交：`836e5c99d2ec7e6f5ea01700f581952370ed788f`

### 本次修复

- 区分画面中的红圈、红框、圈中文字和口播删除意见；只有飞书富文本实际标色的文字才进入口播删除流程。
- 修正“提前到/改到”类意见的标签时间，使用目标时间并保留原时间与目标时间的审计信息。
- 增强中文省略句、连续口吃以及中文数字与阿拉伯数字差异下的 ASR 定位，同时保持时间附近唯一匹配和连续语音边界约束。
- 改进反向 ASR 的必留词保护、边界漂移判断和重复词裁决，避免把校验通过误报为全部删除已执行。
- 完成报告明确区分实际音频切口、仅标签未执行项目和整体草稿/封包验收结果。

### Lite 交付边界

- 口播删除仍采用非破坏性的可编辑切口记录，源媒体保持完整，工程总时长不变。
- 暂停、画面动画和无法安全判定的意见保留为原文标签，不伪造切口。
- 本包不包含用户凭据、飞书授权状态或本机素材；首次使用时由目标电脑重新配置。
