interface:
  display_name: "Auto-Cut 最终验收"
  short_description: "按 Lite 标签、默认几何、音频和可编辑结构验收剪映草稿"
  default_prompt: "Use $auto-cut-final-acceptance to validate the fixed Lite label-only and default-geometry visual contract together with audio, source coverage, editable structure, and package integrity."

policy:
  allow_implicit_invocation: true
