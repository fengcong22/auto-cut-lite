interface:
  display_name: "Auto-Cut Lite 本地图片"
  short_description: "把提供的 PNG/JPG 按默认几何贴入，不做精准覆盖、缩放或清理"
  default_prompt: "Use $auto-cut-local-image-overlay-revision to insert a supplied PNG/JPG as an editable Lite asset at default geometry and the requested start."

policy:
  allow_implicit_invocation: true
