interface:
  display_name: "Auto-Cut 指向物配置建档"
  short_description: "仅在用户明确要求时管理指向物配置；不是 Lite 编辑门禁"
  default_prompt: "Use $auto-cut-profile-onboarding only for an explicit profile administration request; never invoke it automatically for a Lite edit."

policy:
  allow_implicit_invocation: true
