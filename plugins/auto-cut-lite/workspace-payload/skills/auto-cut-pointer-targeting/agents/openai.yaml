interface:
  display_name: "Auto-Cut Lite 指向物"
  short_description: "提供的指向素材按默认几何贴入；原小手遮挡和清理只贴标签"
  default_prompt: "Use $auto-cut-pointer-targeting to insert a supplied Lite pointer at its requested start with default geometry and no keyframes; keep occlusion or cleanup requests label-only."

policy:
  allow_implicit_invocation: true
